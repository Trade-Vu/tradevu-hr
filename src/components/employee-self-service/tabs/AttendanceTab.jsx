import React from 'react';
import { format } from 'date-fns';
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';
export default function AttendanceTab({ attendance }) { const month = attendance.filter((item) => { const date = new Date(item.date); const now = new Date(); return date.getMonth() === now.getMonth() && date.getFullYear() === now.getFullYear(); }); const stats = [['present', 'Present', 'bg-green-50 text-green-700'], ['absent', 'Absent', 'bg-red-50 text-red-700'], ['late', 'Late', 'bg-yellow-50 text-yellow-700'], ['leave', 'On Leave', 'bg-blue-50 text-blue-700']]; return <Card className="overflow-hidden shadow-sm border-slate-200/60 bg-white/70 backdrop-blur-md rounded-2xl"><CardHeader className="border-b border-slate-200"><CardTitle>My Attendance - {format(new Date(), 'MMMM yyyy')}</CardTitle></CardHeader><CardContent className="p-6"><div className="grid gap-4 mb-6 md:grid-cols-4">{stats.map(([status, label, className]) => <div key={status} className={`p-4 text-center rounded-lg ${className}`}><p className="text-3xl font-bold">{month.filter((item) => item.status === status).length}</p><p className="text-sm text-slate-600">{label}</p></div>)}</div></CardContent></Card>; }