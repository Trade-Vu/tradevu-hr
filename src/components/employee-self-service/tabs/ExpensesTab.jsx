import React from 'react';
import { format } from 'date-fns';
import { Receipt } from 'lucide-react';
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';
import { Badge } from '@/components/ui/badge';
export default function ExpensesTab({ expenses }) { return <Card className="overflow-hidden shadow-sm border-slate-200/60 bg-white/70 backdrop-blur-md rounded-2xl"><CardHeader className="border-b border-slate-200"><CardTitle>My Expense Claims</CardTitle></CardHeader><CardContent className="p-6">{expenses.length === 0 ? <div className="py-12 text-center"><Receipt className="w-16 h-16 mx-auto mb-4 text-slate-300" /><p className="text-slate-500">No expense claims</p></div> : <div className="space-y-3">{expenses.map((expense) => <div key={expense.id} className="flex items-center justify-between p-4 rounded-lg bg-slate-50"><div><p className="font-semibold capitalize text-slate-900">{expense.expense_type.replace('_', ' ')}</p><p className="text-sm text-slate-600">{format(new Date(expense.date), 'MMM dd, yyyy')} • {expense.amount} SAR</p><p className="text-xs text-slate-500">{expense.description}</p></div><Badge className={expense.status === 'approved' ? 'bg-green-100 text-green-700' : expense.status === 'rejected' ? 'bg-red-100 text-red-700' : expense.status === 'reimbursed' ? 'bg-blue-100 text-blue-700' : 'bg-yellow-100 text-yellow-700'}>{expense.status}</Badge></div>)}</div>}</CardContent></Card>; }