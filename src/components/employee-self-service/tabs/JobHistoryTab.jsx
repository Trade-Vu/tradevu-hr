import React from 'react';
import { Clock } from 'lucide-react';
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';
import { formatStatusDate, renderStatusHistoryBadge } from '../employeeSelfServiceUtils.jsx';
export default function JobHistoryTab({ employee, statusHistory }) { return <Card className="overflow-hidden border-none shadow-xl shadow-slate-200/40 rounded-2xl bg-white/60 backdrop-blur-xl"><CardHeader className="px-8 pt-8 pb-6 border-b border-slate-100 bg-white/50"><CardTitle className="text-xl text-slate-800">Lifecycle & History</CardTitle></CardHeader><CardContent className="p-8 space-y-12"><section className="space-y-4"><h4 className="text-lg font-medium text-slate-800">Promotion History</h4>{employee.promotion_history?.length ? <div className="overflow-hidden bg-white border shadow-sm border-slate-200 rounded-xl"><table className="w-full text-sm text-left"><thead className="border-b bg-slate-50 text-slate-500 border-slate-200"><tr><th className="px-6 py-4 font-medium">Date</th><th className="px-6 py-4 font-medium">Previous Role</th><th className="px-6 py-4 font-medium">New Role</th></tr></thead><tbody className="divide-y divide-slate-100">{employee.promotion_history.map((item) => <tr key={item.id} className="hover:bg-slate-50"><td className="px-6 py-4 text-slate-600">{new Date(item.createdAt).toLocaleDateString()}</td><td className="px-6 py-4 text-slate-600"><div className="font-medium text-slate-800">{item.previousTitle}</div><div className="mt-1 text-xs text-slate-500">{item.previousGrade}</div></td><td className="px-6 py-4"><div className="font-medium text-indigo-700">{item.newTitle}</div><div className="mt-1 text-xs text-indigo-500/70">{item.newGrade}</div></td></tr>)}</tbody></table></div> : <EmptyHistory text="No promotion history found." />}</section><section className="space-y-4"><h4 className="text-lg font-medium text-slate-800">Status History</h4>{statusHistory.length ? <div className="overflow-hidden bg-white border shadow-sm border-slate-200 rounded-xl"><table className="w-full text-sm text-left"><thead className="border-b bg-slate-50 border-slate-200"><tr><th className="px-6 py-4 font-medium text-slate-600">Date</th><th className="px-6 py-4 font-medium text-slate-600">Previous Status</th><th className="px-6 py-4 font-medium text-slate-600">New Status</th><th className="px-6 py-4 font-medium text-slate-600">Reason</th></tr></thead><tbody className="divide-y divide-slate-100">{statusHistory.map((item, index) => <tr key={item.id || item._id || `status-${index}`} className="hover:bg-slate-50"><td className="px-6 py-4 font-mono text-xs text-slate-600">{formatStatusDate(item.createdAt || item.date || item.timestamp || item.updatedAt)}</td><td className="px-6 py-4">{renderStatusHistoryBadge(item.previousStatus || item.from || 'N/A')}</td><td className="px-6 py-4">{renderStatusHistoryBadge(item.newStatus || item.to || 'N/A', true)}</td><td className="px-6 py-4 text-xs text-slate-600">{item.reason || 'N/A'}</td></tr>)}</tbody></table></div> : <EmptyHistory text="No status history found." />}</section></CardContent></Card>; }
function EmptyHistory({ text }) { return <div className="p-8 text-center border border-dashed border-slate-200 rounded-xl bg-slate-50/50 text-slate-500"><Clock className="w-12 h-12 mx-auto mb-3 text-slate-300" /><p>{text}</p></div>; }